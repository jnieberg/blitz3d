//TO BE DONE
function _updategamma() {
	// const imageData = _graphicsBuffer.context.getImageData(0, 0, _graphicsBuffer.canvas.width, _graphicsBuffer.canvas.height);
	// const data = imageData.data;
	// for (var i = 0; i < data.length; i += 4) {
	// 	data[i] = _setGammaDestRed;
	// 	data[i + 1] = _setGammaDestGreen;
	// 	data[i + 2] = _setGammaDestBlue;
	// }
	// _graphicsBuffer.context.putImageData(imageData, 0, 0);
}